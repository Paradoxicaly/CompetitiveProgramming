#include <bits/stdc++.h>
using namespace std;

int main() {
	int n = 0;
    cin >> n;

    vector<pair<int, int>> pairs;  

    for (int i = 0; i < n; i++) {
        int y, x;
        cin >> y >> x;  
        pairs.push_back({y, x});
    }
    
}